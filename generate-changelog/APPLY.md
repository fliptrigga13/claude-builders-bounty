# Apply (manual — API write blocked from this agent)

1. On https://github.com/claude-builders-bounty/claude-builders-bounty/issues/1 comment exactly:
   `/opire try`
2. Fork the repo to `flipperspectives-crypto`.
3. Add these files from `/workspace/claude-builders-changelog/`:
   - `changelog.sh`
   - `skills/generate-changelog/SKILL.md`
   - `README.md`
   - `samples/CHANGELOG.sample.md`
4. Open a PR titled: `feat: generate-changelog skill + bash script ($50 bounty #1)`
5. Link the issue in the PR body.

If GitHub App access is granted for that repo, ask Computer to finish the claim + PR automatically.
